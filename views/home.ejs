<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">

        <div class="form p-10 text-white">

            <form action="/create" method="post">     
                <input class="block px-5 py-2 w-full rounded-md outline-none  bg-zinc-700" type="text" placeholder="Title goes here" name="title">
                <textarea class="block mt-2 px-5 py-2 w-full rounded-md outline-none  bg-zinc-700 resize-none" placeholder="Description goes here" name="description"></textarea>
                <input class="mt-2 bg-blue-500 px-5 py-2 rounded-md cursor-pointer" type="submit" value="Add Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-3 flex-wrap">

            <% if(files.length > 0) { %>
                <% files.forEach(function(file) { %>
                    <div class="task min-w-72 px-3 py-2 rounded-md bg-zinc-700">
                        <h1 class="text-white text-2xl tracking-tighter"><%= file.replace(".txt","") %></h1>
                        <div class="mt-3 flex justify-between aligner-items-center ">
                            <a class="text-blue-500 inline-block cursor-pointer" href="/files/<%= file %>">Read more...</a>
                            <a class="text-zinc-400 inline-block cursor-pointer" href="/edit/<%= file %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            
            <% } else { %>
                <h3 class="text-zinc-500">No tasks yet.</h3>
            <% } %>
            
        </div>
    </div>

</body>
</html>